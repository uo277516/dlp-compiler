def main() do
    name :: [10 :: char]
    letter :: char

    name[0] = '1'
    name[0] = name['a'] # Error invalid indexing expression
    letter = name[2.2] # Error invalid indexing expression

    puts letter[1] # Error invalid indexing
end
